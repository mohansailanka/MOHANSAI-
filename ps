class Palindrome:
    def is_palindrome(self, number):
        number_str = str(number)
        if number_str == number_str[::-1]:
            return True
        else:
            return False
number = int(input("Enter a number: "))
pal = Palindrome()
if pal.is_palindrome(number):
    print(f"{number} is a palindrome!")
else:
    print(f"{number} is not a palindrome.")
